create external table ${tableName} (
#foreach($column in $columns) `${column.columnName}`  $column.attrType #if($foreach.hasNext),#end #end
 )
row format delimited
fields terminated by '\t'
stored as textfile
location '/health/hbs/${tableName}';
